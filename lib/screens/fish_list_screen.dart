import 'package:flutter/material.dart';

class FishListScreen extends StatelessWidget {
  const FishListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fish list Screen', style: TextStyle(fontSize: 24)),
    );
  }
}